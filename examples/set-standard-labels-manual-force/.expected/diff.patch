diff --git a/Kptfile b/Kptfile
index 35616b4..038facc 100644
--- a/Kptfile
+++ b/Kptfile
@@ -8,4 +8,24 @@ pipeline:
   mutators:
     - image: ghcr.io/kptdev/krm-functions-catalog/set-standard-labels:latest
       configMap:
-        isDeployment: false
+        isDeployment: "false"
+status:
+  conditions:
+    - type: Rendered
+      status: "False"
+      reason: RenderFailed
+      message: |-
+        pkg.render: pkg .:
+        	pipeline.run: already handled error
+  renderStatus:
+    mutationSteps:
+      - image: ghcr.io/kptdev/krm-functions-catalog/set-standard-labels:latest
+        stderr: 'failed to evaluate function: error: function failure'
+        exitCode: 1
+        results:
+          - message: unable to assign the given ConfigMap `.data` to FunctionConfig SetStandardLabels. please make sure the SetStandardLabels has a field of type map[string]string
+            severity: error
+        errorResults:
+          - message: unable to assign the given ConfigMap `.data` to FunctionConfig SetStandardLabels. please make sure the SetStandardLabels has a field of type map[string]string
+            severity: error
+    errorSummary: 'ghcr.io/kptdev/krm-functions-catalog/set-standard-labels:latest: exit code 1'
